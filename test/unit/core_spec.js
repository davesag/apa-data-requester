import {Map, List} from 'immutable';
import {expect} from 'chai';
import {INITIAL_STATE} from '../../src/core'

describe('application logic', () => {

  // describe('loadData', () => {
  //   const payload = Map({test: 'test'});
  //   const nextState = loadedData(INITIAL_STATE, payload);
  //   const expected  = INITIAL_STATE.mergeDeep(payload);
  //
  //   it('adds the payload to the state', () => {
  //     expect(nextState).to.equal(expected);
  //   });
  //
  // });
});
